create cluster:
    HassAPI.Inpute_Command_function
    Hass.createCluster
        //ClusterManager.createCluster
        if success:
            if nodeList not null:
                //ClusterManager.addNode
        else:
            return fail


    ClusterManager.createCluster
        //if name is overlap:
            return fail

        //create cluster
            create cluster_uuid
            clusterList[cluster_uuid]= cluster

    ClusterManager.addNode
        //get cluster by cluster_uuid
        //Cluster.addNode


    Cluster.addNode(input:nodeList)
        //for

